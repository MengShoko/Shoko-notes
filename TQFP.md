**TQFP** 是 **Thin Quad Flat Package** 的缩写，中文称为**薄型四方扁平封装**。

它和你刚才问的 **[[LQFP]]** (Low-profile Quad Flat Package) 非常相似，都是四边有引脚的表面贴装封装，但在**厚度**和**引脚间距**上有一些关键区别。

### 1. 核心含义拆解

- **T (Thin)**：**薄型**。这是它与普通 QFP 最大的区别，强调封装厚度较薄（通常主体厚度在 1.0mm 左右，含引脚总高约 1.2mm）。
- **Q (Quad)**：**四方**。引脚分布在芯片的四个侧面。
- **F (Flat)**：**扁平**。引脚水平伸出，贴装在 PCB 表面。
- **P (Package)**：**封装**。

### 2. TQFP vs. LQFP：有什么区别？

这是最容易混淆的地方。在 STM32 和大多数现代 MCU 中，**LQFP 和 TQFP 经常指代非常相似的封装**，甚至在不同厂商的数据手册中混用。但严格来说：

|特性|**TQFP** (Thin QFP)|**LQFP** (Low-profile QFP)|**备注**|
|---|---|---|---|
|**主要定义**|强调**整体厚度薄**。|强调**低轮廓**（JEDEC 标准术语）。|在现代语境下，两者几乎通用。|
|**典型厚度**|本体约 1.0mm，总高约 1.2mm。|本体约 1.4mm，总高约 1.6mm (早期标准)，但现在 LQFP 也做得很薄。|**LQFP 目前更主流**，尤其是 STM32 系列。|
|**引脚间距**|常见 0.5mm, 0.4mm, 0.3mm。|常见 0.5mm, 0.8mm。|TQFP 常用于引脚更密、更薄的场景。|
|**应用场景**|对厚度敏感的便携设备、早期 ARM 芯片。|**STM32 的标准封装** (型号后缀为 **T**)。|ST 官方数据手册通常称其 F1/F4 系列封装为 LQFP。|
|**兼容性**|如果引脚数和间距相同，通常可以直接互换焊接。|同上。|需确认具体尺寸图纸。|

> **💡 关键点**：在 **STM32** 的语境下，官方数据手册（Datasheet）通常将那种 0.5mm 间距、四边引脚的封装称为 **LQFP**（对应型号中的 `T`，如 `C8T6`）。如果你看到 **TQFP**，它通常指代物理尺寸几乎一样的封装，可能是其他厂商（如 NXP, TI, Microchip）的叫法，或者是更早期的叫法。**它们在焊接和 PCB 封装设计上通常是兼容的（只要引脚数和 Pitch 一致）。**

### 3. TQFP 的常见规格

TQFP 封装通常用 **引脚数** 来区分大小，例如：

- **TQFP-32**: 32 个引脚 (常见于小容量 STM32F0/F1)
- **TQFP-48**: 48 个引脚 (对应 STM32 的 `C` 系列，如 F103C8)
- **TQFP-64**: 64 个引脚 (对应 STM32 的 `R` 系列)
- **TQFP-100**: 100 个引脚 (对应 STM32 的 `V` 系列，如 F103VE)
- **TQFP-144**: 144 个引脚 (对应 STM32 的 `Z` 系列)

**引脚间距 (Pitch)** 最常见的是 **0.5mm**，也有 0.4mm (更密) 或 0.8mm (较疏)。

### 4. 为什么会有 TQFP 和 LQFP 两种叫法？

- **历史演变**：早期 JEDEC 标准定义了 QFP，后来为了更薄推出了 TQFP。随后为了规范低高度封装，又定义了 LQFP。
- **厂商习惯**：
- **ST (意法半导体)**：偏好使用 **LQFP** 这个词（如 STM32F103C**T**6 中的 T 代表 LQFP）。
- **TI (德州仪器)、NXP (恩智浦)**：经常使用 **TQFP** 这个词。
- **实际物理尺寸**：对于 48 脚或 100 脚、0.5mm 间距的芯片，TQFP 和 LQFP 的 footprint（PCB 焊盘设计）通常是**完全一样**的。

### 5. 总结

- **TQFP** = **薄型四方扁平封装**。
- 它与 **LQFP** 极度相似，在很多情况下可以视为**同一种封装的不同叫法**。
- 在 **STM32** 选型中，你主要会看到 **LQFP**（型号后缀 `T`），但如果你买到的芯片标着 TQFP，只要**引脚数**和**间距（如 0.5mm）**对得上，就可以直接用在为 LQFP 设计的电路板上。
- **焊接难度**：与 LQFP 一样，0.5mm 间距需要一定的拖焊技巧；0.4mm 间距则较难手工焊接。

**一句话结论**：TQFP 和 LQFP 是“亲兄弟”，长得几乎一样，只是名字不同，**在绝大多数 STM32 应用中可以互换使用**。