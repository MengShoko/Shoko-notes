一种用于强化学习的算法，适用于：
连续动作控制
机器人控制
移动机器人导航
无人车控制
机器臂控制
自动驾驶


SAC的目标是训练一个神经网络,让机器人学会:
根据当前环境状态，输出合适的控制动作，
使机器人尽可能安全、快速、平稳地到达目标。

Actor：演员网络
根据状态s，产生动作a
a ~ πφ(a|s)
其中：

- `πφ` 是 Actor 策略；
- `φ` 是 Actor 网络参数；
- `s` 是状态；
- `a` 是动作。

Actor并不一定输出一个固定动作，而是输出一个动作分布
```
Actor 输出：
    动作均值 μ = [0.08, 0.20]
    动作标准差 σ = [0.03, 0.10]
```
然后从这个分布中采样：

```
v ~ Normal(0.08, 0.03)
ω ~ Normal(0.20, 0.10)
```
这样策略就天然具有探索能力

### Critic：评论家网络
它通过估计Q（s，a）
在状态s下执行动作a，从现在开始预计能够获得多少累计回报。

### Actor网络
Actor根据Critic的评价逐渐调整自己的动作分布
SAC使用两个两个Critic，计算目标时取两者的较小值，这称为Twin Critic
这是由于神经网络估计Q值时很容易高估，取较小值可以降低过度乐观的问题

## SAC训练过程：
#### 一、机器人与环境
读取当前状态s_t
Actor网络根据s_t生成一组动作分布，然后从这组动作中采样a_t
环境执行动作后返回s_t+1,r_t,done，
于是得到一条经验(s_t, a_t, r_t, s_{t+1}, done)

#### 二、把经验存入Replay Buffer
SAC是一种off-policy算法
它会把经验存放到经验回放池
结构类似：
ReplayBuffer = [
    (s0, a0, r0, s1, done0),
    (s1, a1, r1, s2, done1),
    (s2, a2, r2, s3, done2),
    ...
]
配置中
replay_capacity = 500000
表示最多保存50w条经验
之后训练时不一定使用刚刚产生的数据，而是从整个缓冲区中随机抽取一个batch

#### 三、随机抽取一个batch
每次抽取的样本数量取决于batchsize


#### 四、计算Critic 的目标值
下一状态 `s'` 进入当前 Actor，采样下一步动作：
*这里的s‘指的是batchsize中提取数据中的s'*

```
a' ~ π(a'|s')
```

然后用目标 Critic 计算：

```
y =
r + γ(1-done)[
min(Q1_target(s',a'), Q2_target(s',a'))- α log π(a'|s')
]
```
r表示当前动作立即获得的奖励
γ表示未来奖励的折扣系数

#### 五、更新两个Critic
通过均方误差来更新，使得Critic的估计更接近目标值
```
L_Q1 =
E [
    (Q1(s,a) - y)²
]
```

#### 六、更新Actor

Actor的目的是最大化Q值，同时保留一定熵
常见的损失形式是：
```
L_actor =
E [
    α log π(a|s) - min(Q1(s,a), Q2(s,a))
]
```

#### 七、自动调节熵系数α
#### 八、软更新目标Critic
目标网络Q1_target,Q2-target会进行软更新
```
θ_target ← τ θ + (1-τ) θ_target
```
