Self-Organizing Map，自组织映射（亦称为自组织特征映射，Self-Organizing Feature Map），Kohonen网络，是一种竞争学习型的无监督神经网络，能将高维输入数据映射到低维空间（通常是二维），同时保持输入数据在高维空间的拓朴结构，即将高维空间中相似的样本点映射到网络输出层中的邻近神经元

SOM网络中的输出层神经元以矩阵方式排列在二维空间中，每个神经元都拥有一个权向量，网络在接收输入向量后，将会确定输出层获胜神经元，它决定了输入向量在低维空间的位置。SOM的训练目标就是为每个输出层神经元找到合适的权向量，以达到保持拓扑结构的目的。

### 1. 核心概念与结构

SOM 网络主要由两层组成：

1. **输入层 (Input Layer)**：接收高维数据向量（例如：X=[x1,x2,…,xm]X=[x1​,x2​,…,xm​]）。
2. **竞争层/输出层 (Competitive/Output Layer)**：通常是一个二维的网格（如 10×1010×10 的节点矩阵）。每个节点都有一个与输入向量维度相同的**权重向量 (Weight Vector)**。

#### 工作原理：竞争与合作

SOM 的训练过程模拟了生物神经系统的“胜者通吃 (Winner-Takes-All)"机制，但加入了“邻域合作”：

1. **竞争 (Competition)**：当一个输入样本进入网络，所有输出节点计算该样本与自己权重向量的距离（通常是欧氏距离）。距离最小的节点成为**最佳匹配单元 (BMU, Best Matching Unit)**，即“获胜者”。
2. **合作 (Cooperation)**：不仅 BMU 更新权重，**BMU 周围的邻居节点**也会更新权重。
3. **适应 (Adaptation)**：邻居节点权重更新的方向是靠近输入样本，但更新幅度随距离 BMU 的远近而衰减（距离越远，更新越小）。

---

### 2. 算法流程 (训练步骤)

假设输入向量为 XX，节点 ii 的权重为 WiWi​：

1. **初始化**：随机初始化所有节点的权重向量。
2. **采样**：从训练集中随机选取一个样本 XX。
3. **寻找 BMU**：计算 XX 与所有节点权重的距离，找到距离最小的节点 cc (BMU)： c=arg⁡min⁡i∥X−Wi∥c=argmini​∥X−Wi​∥
4. **更新权重**：更新 BMU 及其邻域 Nc(t)Nc​(t) 内节点的权重： Wi(t+1)=Wi(t)+η(t)⋅hci(t)⋅(X−Wi(t))Wi​(t+1)=Wi​(t)+η(t)⋅hci​(t)⋅(X−Wi​(t)) 其中：

- η(t)η(t)：**学习率**，随时间 tt 逐渐减小。
- hci(t)hci​(t)：**邻域函数** (通常是高斯函数)，表示节点 ii 与 BMU cc 的拓扑距离。距离越近，hh 越大；随时间推移，邻域半径也会逐渐缩小。

5. **迭代**：重复步骤 2-4，直到收敛（权重不再显著变化或达到最大迭代次数）。

---

### 3. SOM 的核心特性

- **无监督学习**：不需要标签数据，自动发现数据的内在结构。
- **拓扑保持 (Topology Preservation)**：这是 SOM 最独特的性质。如果两个数据在高维空间很相似，它们在 SOM 网格上的位置也会很近。这使得 SOM 非常适合**可视化**。
- **降维**：将高维数据（如 100 维特征）压缩到 2D 平面上展示，同时保留聚类信息。
- **聚类与分类**：网格上相邻的区域通常代表同一类数据，自然形成了聚类边界。

---

### 4. 典型应用场景

1. **数据可视化**：

- 将高维数据（如基因表达数据、文档向量）映射到 2D 地图，直观看到数据的分布和聚类情况。

2. **聚类分析**：

- 客户细分、图像分割、文档分类。

3. **异常检测**：

- 正常数据会聚集在某些区域，而异常数据会落在权重更新很少的“边缘”节点或产生很大的量化误差。

4. **矢量量化 (Vector Quantization)**：

- 用于数据压缩，用少量的码本（网格节点权重）代表大量数据。

---

### 5. SOM 与其他算法的对比

|特性|**SOM (自组织映射)**|**K-Means**|**PCA (主成分分析)**|
|---|---|---|---|
|**学习类型**|无监督|无监督|无监督 (线性)|
|**输出形式**|**二维网格** (保留拓扑)|离散簇标签|低维坐标 (线性投影)|
|**拓扑保持**|**强** (相邻节点代表相似类)|无 (簇之间无空间关系)|弱 (仅保留方差最大方向)|
|**可视化能力**|**极强** (可直接画图观察)|弱 (需额外降维才能画图)|强 (但仅限线性关系)|
|**计算复杂度**|较高 (需迭代更新邻域)|较低|低|