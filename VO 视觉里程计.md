Visual Odometry 仅依赖相机（视觉传感器）来估计相机自身的运动轨迹和周围环境结构的技术
### 1.核心定义
VO的目标是分析连续图像帧之间的特征变化，计算出相机在三维空间中的**相对位移**和旋转
输入：单目相机（monocular）或双目相机（stereo）拍摄的图像序列
输出：相机随时间变化的轨迹和稀疏/稠密的地图
### 2.工作原理
#### 前端（视觉特征处理）
* 特征提取，从图像中提取关键点（如角点，边缘，常用算法ORB,SIFT,SURF)
* 特征匹配：从连续两帧图像中找到相同的特征点
* 运动估计：利用几何原理（如对极几何，Pnp算法）计算相机的运动矩阵，如果是双目相机，还可以直接通过视差计算深度
#### 后端（优化）
由于噪声和匹配误差，直接计算的运动会有偏差。后疃通过束调整（Bundle Adjustment BA）或滤波（如EKF）来优化轨迹，最小化重投影误差，使轨迹更加平滑准确

#### 3.主要优缺点
成本低：只需普通的摄像头，无需昂贵的激光雷达或高精度IMU
信息丰富：不仅能定位，还能重建环境的纹理和结构（用于建图）
无累计漂移：如果结合回环测试（Loop Closure，即认出“我回到过这里”），可以消除长期累积误差，升级为Visual SLAM
计算量大：图形处理非常消耗算力
环境依赖：怕黑，怕反光，怕纯色墙面，怕运动模糊