---
title: STM32 软硬件基础与最小系统设计
created: 2026-09-01
type: course-note
course: 嵌入式系统
tags:
  - 嵌入式硬件
  - STM32
  - 微控制器
  - 最小系统
sources:
  - 嵌入式系统课程笔记
confidence: high
status: in-progress
---

## STM概述

STM32是意法半导体（STMicroelectronics）较早推向市场的基于Cortex-M内核的微处理器系列产品，该系列产品具有成本低、功耗优、性能高、功能多等优势，并且以系列化方式推出，方便用户选型，在市场上获得了广泛好评。

STM32 目前常用的有STM32F103～107系列，简称"1系列"，最近又推出了高端系列 STM32F4xx系列，简称"4系列"。前者基于Cortex-M3内核，后者基于Cortex-M4内核。

STM32F4xx系列在以下诸多方面做了优化：
1. 增加了浮点运算
2. DSP处理
3. 存储空间更大，高达1M字节以上
4. 运算速度更高，以168MHz高速运行时可达到210DMIPS的处理能力
5. 更高级的外设，新增外设，例如，照相机接口、加密处理器、USB 高速OTG 接口等

[[STM命名规则]]

## 一、STM32F10x系列单片机特点

STM32F10x系列单片机基于ARM Cortex-M3内核，其结构特点为：
1. 一个主晶振可以驱动整个系统，低成本的4～16MHz晶振即可驱动CPU、USB和其他所有外设。
2. 内嵌出厂前调校好的8MHz RC振荡器，可以作为低成本主时钟源。
3. 内嵌电源监视器，减少对外部器件的要求，提供上电复位、低电压检测、掉电检测。
4. GPIO：最大翻转频率为18MHz。
5. PWM定时器：可以接收最大72MHz时钟输入。
6. USART：传输速率可达4.5Mbit/s。
7. ADC：12位，转换时间最快为1μs。
8. DAC：提供2个通道，12位。
9. SPI：传输速率可达18Mbit/s，支持主模式和从模式。
10. I2C：工作频率可达400kHz。
11. I2S：采样频率可选范围为8～48kHz。
12. 自带时钟的看门狗定时器。
13. USB：传输速率可达12Mbit/s。
14. SDIO：传输速率为48MHz。

## 二、最小系统设计

==最小系统是指能够让单片机正常工作的包含最少元器件的系统==

### 1. 电源电路
为整个电路系统提供所需的供电电压

### 2. 调试电路
为MCU提供程序下载和调试的接口

### 3. 时钟电路
为MCU提供所需的时钟信号

### 4. 复位电路
为MCU提供统一的初始状态

![[Pasted image 20260831141558.png]]

## 三、时钟源

STM32系列微控制器中，有4个主要时钟源，分别是：

| 编号 | 名称 | 说明 |
|------|------|------|
| 1 | **高速内部时钟 (HSI)** | 由芯片内部的RC振荡器提供，默认频率为16MHz |
| 2 | **高速外部时钟 (HSE)** | 通过在OSC_IN和OSC_OUT引脚接入晶振实现，频率范围为4MHz-26MHz |
| 3 | **低速内部时钟 (LSI)** | 由芯片内部的RC振荡器提供，默认频率为32kHz |
| 4 | **低速外部时钟 (LSE)** | 通过在OSC_IN和OSC_OUT引脚接入32.768KHz晶振实现 |

![[Pasted image 20260831142035.png]]

- **无源晶振**：2个引脚；内部无电源，需CPU内部振荡器才能产生时钟信号；便宜
- **有源晶振**：4个引脚；内部有震荡电路，通电直接输出时钟信号；稍贵

## 四、启动配置引脚

| BOOT1 | BOOT2 | 启动模式 |
|-------|-------|---------|
| X | 0 | 主闪存存储器 |
| 0 | 1 | 系统存储器 |
| 1 | 1 | 内置SRAM |

## 五、开发环境搭建

进行STM32F系列微控制器的程序开发需要搭建一个交叉开发环境，包括计算机、开发软件、调试器、开发板或自己设计的电路板（包括STM32F系列微控制器）。

### 1. 调试器类型
- **JLINK**：J-Link 是SEGGER 公司为支持仿真ARM 内核芯片推出的[[JTAG]] 仿真器。是通用的开发工具，可以用于KEIL、IAR、ADS等平台。
- **ULINK**：ULINK是KEIL公司开发的仿真器，专用于KEIL MDK平台。
- **STlink**：STLink 是ST 公司为STM8和 STM32系列MCU设计的调试器。

### 2. MDK 开发环境
MDK 全称为Microcontroller Development Kit，即微控制器开发套件。它源自Keil 公司（后被ARM 公司收购），现也称为MDK-ARM。

### 3. STM32CubeMX
STM32CubeMX 是一种图形化的配置工具，通过分步过程可以非常轻松地配置STM32 微控制器和微处理器，以及为ARM Cortex-M 内核或面向ARM Cortex-A 内核的特定Linux 设备树生成相应的初始化C代码。

### 4. STM32 标准库
STM32 库是由ST公司针对STM32提供的函数接口，即API (Application Program Interface)，开发者可调用这些函数接口来配置STM32的寄存器。

## 六、CMSIS 标准

STM32的内核是ARM公司设计的处理器体系架构。ST公司或其它芯片生产厂商，负责设计的是在内核之外的部件，被称为核外外设或片上外设、设备外设。

为了解决不同的芯片厂商生产的Cortex微控制器软件的兼容性问题，ARM与芯片厂商建立了[[CMSIS]]标准 (Cortex MicroController Software Interface Standard)。

CMSIS标准中最主要的是CMSIS核心层，它包括了：
1. **内核函数层**：其中包含用于访问内核寄存器的名称、地址定义，主要由ARM公司提供。
2. **设备外设访问层**：提供了片上的核外外设的地址和中断定义，主要由芯片生产商提供。

CMSIS层位于硬件层与操作系统或用户层之间，提供了与芯片生产商无关的硬件抽象层，可以为接口外设、实时操作系统提供简单的处理器软件接口，屏蔽了硬件差异。

STM32的库，就是按照CMSIS标准建立的。

## 七、存储器映射与寄存器

把片上外设对应的寄存器在存储空间上分配地址的过程就叫**寄存器映射**。
与存储单元一样，每个寄存器（一般是32位的）都有一个寻址地址。
应用程序对片上外设的初始化和控制就是通过对片上外设对应一系列寄存器的修改读写实现的。

需要哪一个片上外设即将库中对应的源文件添加到工程中，同时包含相应头文件。

---

## 相关知识点
- [[STM32命名规则]] - STM32芯片型号命名规则
- [[JTAG 边界扫描调试接口]] - JTAG调试器原理
- [[CMSIS]] - ARM Cortex-M标准外设库接口
- [[GPIO 通用输入输出端口]] - GPIO配置
- [[GND 地线]] - 电源地设计
- [[BGA 球栅阵列封装]] - 高端STM32封装类型
