拆解法：把一个多分类任务拆解为多个二分类任务完成
OvO
通过N个标签的类别将数据分成N类，然后训练N（N-1）/2的训练器，将C1到CN两两配对，最后将测试样本交给所有训练器来测试，出现最多的即为预测结果
训练N（N-1）/2个分类器，存储开销和测试时间大
训练只用两个类的样例，训练时间短
OvR
将一个和剩下的做比较，预测结果为T/F，结果为T是预测结果
* 训练N给分类器，存储开销和测试时间小
* 训练用到全部训练样例，训练时间长*
预测性能取决于训练数据，通常情况下二者差不多

