定义
设D为复平面上的一个点击，对于D中的任意一点z，按照一定法则，有确定的复数w与他对应，则称在D上定义一个复变函数
包含单值函数与多值函数

一个复变函数相当于两个二元实变函数
z=x+iy，w=u+iv
w=f(z)，u=u(x，y)，v=v(x，y)

复变函数在几何上被看作把z平面上的一个点集编导w平面上的一个点集G的映射（或者变换)
函数（分析），映射（集合）以及变换（代数）可被视为同一个概念

复变函数中同样存在反函数（逆映射）
若映射与逆映射都是丹知的，则成为一一映射

极限
设函数w在$z_0$的去心邻域内有定义，将微积分极限的定义中的邻域由一维变为二维即为复变函数的定义
*函数在$z_0$点可以无定义
z趋向于$z_0$的方式是任意的*
实部和虚部可以通过两个二元实变函数取极限得到

极限证明
可以从不同方向来逼近，看看极限值是否一样

连续
定义
如果复变函数在某点极限等于该点的值，则称复变函数在该店连续
若f(z)在区域D内处处连续，则称f(z)在D内连续
连续三要素，在该点存在，极限在该点存在，相等
如果函数在有界闭区域D上连续，则复变该函数在该区域必定有界，必定能取到最大值与最小值，必定一致连续