Memory Management Unit是现代计算机CPU中的一个关键硬件组件，主要负责处理CPU发出的内存访问请求。
## 核心功能
* A.虚拟地址到物理地址的转化
* 这是MMU最基本的功能
* 在现代操作系统中，程序看到的内存地址是虚拟地址（Virtual Address VA），而不是真实的硬件内存地址（物理地址，Physical Address PA）
* 当CPU想要读取某个虚拟地址的数据时，MMU会自动查表，将其装换成实际的物理内存地址
* 简化编程：程序员不需要关心物理内存的具体布局，每个程序都可以认为自己独占连续的内存空间
* 解决碎片化：即使物理内存是碎片化的，MMU也能将其映射为连续的虚拟地址空间
* 
* B.内存保护
* MMU可为每一块内存区域设置访问权限，如只读，科协，可执行或者禁止访问
* 隔离性，它确保一个进程无法非法访问另一个进程或操作系统内核的内存空间，如果系统试图越界访问，
* MMU会立即触发异常（如Segment Fault），防止系统崩溃或者被恶意软件攻击*